let height = undefined;
let result = height || "Height is not defined"

/*function checkValue(result) { 
    if (result == undefined) {
        return ("Height is not defined");
    }
    return result;
}

console.log(checkValue(result)); //  Result ควรจะได้ออกมาเป็น “Height is not defined”*/

console.log(result);
